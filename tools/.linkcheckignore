# Patterns (URL substrings) that linkcheck.py treats as "intentionally
# referenced but not required to resolve." One pattern per line; blank
# lines and lines beginning with # are ignored.
#
# Use sparingly — every line here is a curation debt. Each entry should
# have a reason comment.

# Sibling repos in the curator's four-repo learning constellation —
# referenced as standalone peers in the README footer. Each is created on
# its own timeline; their URLs may 404 transiently. Remove these
# exceptions once those repos exist.
github.com/bettyguo/ai-engineer-roadmap
github.com/bettyguo/harness-engineer-roadmap
github.com/bettyguo/llm-interview-prep

# Specific Medium profile URLs that 403 automated link-checkers even
# though the article is fully accessible to human readers and was
# verified at curation time. Medium aggressively bot-detects on the
# /@username/ paths; the /towardsdatascience/ paths usually work.
# If you add another medium.com/@... URL, verify it manually with the
# `python tools/validate_entries.py` step *before* relying on linkcheck.
medium.com/@maxshapp/grouped-query-attention-gqa-explained-with-code
